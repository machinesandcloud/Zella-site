[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  # Backend API URL - connects to Render backend
  VITE_API_URL = "https://zella-site.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
